{
  "name": "consumer",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "npx esbuild Example.tsx --bundle --outfile=dist/Example.js",
    "start": "node dist/Example.js",
    "clean": "rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "broken-string": "^0.0.11",
    "esbuild": "^0.17.14",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
